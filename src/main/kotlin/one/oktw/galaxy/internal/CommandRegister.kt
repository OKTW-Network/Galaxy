package one.oktw.galaxy.internal

import org.spongepowered.api.Sponge

class CommandRegister {
    init {
        val commandManager = Sponge.getCommandManager()

//        commandManager.register(Main, CommandCreate().spec, "create")
//        commandManager.register(Main, CommandWorld().spec, "world")
//        commandManager.register(Main, CommandRebuild().spec, "rebuild")
//        commandManager.register(Main, CommandInvite().spec, "invite")
//        commandManager.register(Main, CommandRemove().spec, "remove")
//        commandManager.register(Main, CommandHub().spec, "hub")
    }
}
